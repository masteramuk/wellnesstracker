import 'package:flutter/material.dart';

class WellnessValueScreen extends StatelessWidget {
  const WellnessValueScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Wellness Value Screen'),
    );
  }
}
