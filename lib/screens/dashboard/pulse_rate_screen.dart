import 'package:flutter/material.dart';

class PulseRateScreen extends StatelessWidget {
  const PulseRateScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Pulse Rate Screen'),
    );
  }
}
