import 'package:flutter/material.dart';

class SugarMeasurementScreen extends StatelessWidget {
  const SugarMeasurementScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Sugar Measurement Screen'),
    );
  }
}
