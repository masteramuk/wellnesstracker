import 'package:flutter/material.dart';

class CaloriesOutScreen extends StatelessWidget {
  const CaloriesOutScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Calories Out Screen'),
    );
  }
}
