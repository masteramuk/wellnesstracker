import 'package:flutter/material.dart';

class CaloriesInScreen extends StatelessWidget {
  const CaloriesInScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Calories In Screen'),
    );
  }
}
