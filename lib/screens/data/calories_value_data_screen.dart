import 'package:flutter/material.dart';

class CaloriesValueDataScreen extends StatelessWidget {
  const CaloriesValueDataScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Calories Value Data Screen'),
    );
  }
}
