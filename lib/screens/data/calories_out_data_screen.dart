import 'package:flutter/material.dart';

class CaloriesOutDataScreen extends StatelessWidget {
  const CaloriesOutDataScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Calories Out Data Screen'),
    );
  }
}
