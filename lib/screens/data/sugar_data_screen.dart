import 'package:flutter/material.dart';

class SugarDataScreen extends StatelessWidget {
  const SugarDataScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Sugar Data Screen'),
    );
  }
}
