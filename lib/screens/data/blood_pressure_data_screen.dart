import 'package:flutter/material.dart';

class BloodPressureDataScreen extends StatelessWidget {
  const BloodPressureDataScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Blood Pressure Data Screen'),
    );
  }
}
