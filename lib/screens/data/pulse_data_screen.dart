import 'package:flutter/material.dart';

class PulseDataScreen extends StatelessWidget {
  const PulseDataScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Pulse Data Screen'),
    );
  }
}
