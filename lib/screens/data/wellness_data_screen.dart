import 'package:flutter/material.dart';

class WellnessDataScreen extends StatelessWidget {
  const WellnessDataScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Wellness Data Screen'),
    );
  }
}
