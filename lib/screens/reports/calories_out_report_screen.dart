import 'package:flutter/material.dart';

class CaloriesOutReportScreen extends StatelessWidget {
  const CaloriesOutReportScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Calories Out Report Screen'),
    );
  }
}
