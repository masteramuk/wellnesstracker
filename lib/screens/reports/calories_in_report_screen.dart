import 'package:flutter/material.dart';

class CaloriesInReportScreen extends StatelessWidget {
  const CaloriesInReportScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Calories In Report Screen'),
    );
  }
}
