import 'package:flutter/material.dart';

class SugarMeasurementReportScreen extends StatelessWidget {
  const SugarMeasurementReportScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Sugar Measurement Report Screen'),
    );
  }
}
