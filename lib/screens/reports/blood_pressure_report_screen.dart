import 'package:flutter/material.dart';

class BloodPressureReportScreen extends StatelessWidget {
  const BloodPressureReportScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Blood Pressure Report Screen'),
    );
  }
}
